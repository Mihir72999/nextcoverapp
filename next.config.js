/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    unoptimization
    remotePatterns: [
    unoptimized: true,
      {
        protocol: 'https',
        hostname: 'zapvi.in',
       
      }
    ]
  },
      output: 'npm export'
    
}
module.exports = nextConfig
