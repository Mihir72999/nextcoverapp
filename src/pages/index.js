
import { Container, Typography } from '@mui/material'
import Head from 'next/head'
import Image from 'next/image'
import Link from 'next/link'

import { useGlobalContext } from '../../state/context'


import styles from '../styles/Home.module.css'
import axios from 'axios'



export default  function Home() {

  const { image ,data } = useGlobalContext()

 

  return (

    <>
   
      <Head>
        <title>Mihir Patel</title>
        <meta name="description" content="Generated by mihir patel" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/myLogo.jpeg" />

      </Head>
      <main >

        <div className={styles.bg}>
        </div>
        <div className={`${styles.main} heading`}>
          <Container>

            <div  className='heading' >{data.heading}</div>
            <Typography variant='h4'>{data.title}</Typography>
          </Container>
        </div>
      </main>

      <div className={styles.img} >
        <div className={styles.app}  >
          {image.map((e, i) => {
            return<Link href={`${e.href}${e.name}`} style={{textDecoration:'none',color:'black'}} key={i}>
               <div className={styles.imglist}  > 
  

             <Typography className={styles.shape} variant='h5'><p style={{fontSize:'1rem'}}>only</p>₹{e.price}</Typography>
             <Image  className={styles.gear} height={200} width={150} loading='lazy' src={e.image} alt={e.price} />

              <Typography className={styles.typo} variant='h5'>Phone Case</Typography>
            </div>
            </Link>
          })}
        </div>
      </div>
    </>
  )
}
